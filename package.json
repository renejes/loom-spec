{
  "name": "loom-spec-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Node-based architecture spec that lives in your repo. AI-readable, AI-writable.",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter loom-spec dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
