# Handover prompt — continue with timeline step 15e

Copy everything in the fenced block below into a fresh Claude Code chat in the project root. It briefs the assistant on context, current state, conventions, and exactly what to do first.

---

````
I'm continuing work on `loom-spec`, an open-source tool I've been building over the past week. You're picking up where the previous session left off. Read the briefing below, then read three docs in order, then propose a plan for step 15e before coding.

## What loom-spec is (30 seconds)

A node-based architecture spec that lives in a repo. Two file kinds describe an app:

- `.loom/diagrams/*.flow.json` — graph of nodes (components/services/data stores) and edges (request/event/data-write/…). One per subsystem; drill-down navigation between them.
- `.loom/timelines/*.timeline.json` — overlays one diagram with a sequence of timed events. Each event references a node id in the diagram and has `start_ms`, `duration_ms`, optional `code_refs`, `triggered_by`, `tags`, etc.

The tool is a spec layer, NOT an execution layer. Nodes describe, they don't run. The bet: keep the spec in the repo, edited by both humans (in a browser-based node editor + timeline editor) and AI agents (directly via JSON or via an MCP server we ship). Drift detection catches when `code_refs` point at code that no longer exists.

## Where it stands right now

- Published as **`loom-spec@0.1.1`** on npm.
- Repo: https://github.com/renejes/loom-spec
- Working directory you'll be in: `/Users/renejesser/Desktop/Programming - Projekte/graphical-programming`
- pnpm workspace; the package lives in `packages/loom-spec/`.
- Phase 1 is fully shipped (editor for diagrams, MCP server with 10 tools, drift validator, init/install-mcp commands, full docs + npm package + GitHub releases).
- Phase 2 = timeline view, broken into 7 substeps. **15a, 15b, 15c, 15d are done.** **15e is the next target.** 15f and 15g are pending.

## What you should read in (in this order)

1. `documentation/project-status.md` — full picture of what works, the architecture, how it's wired together. Most important read.
2. `documentation/next-steps.md` — what's open. Skim the "Phase 2 backlog" section.
3. `documentation/implementation-plan.md` § **15e** — the detailed plan for the next step (the mini-graph view alongside the timeline, with edge pulse). This is the spec you'll implement against.
4. Skim `packages/loom-spec/src/view/components/TimelineCanvas.tsx` and `TimelineView.tsx` — the patterns established in 15c/15d for drag/playback. 15e will reuse most of them.
5. Skim `packages/loom-spec/src/view/components/DiagramCanvas.tsx` — the canvas you'll add a read-only mode to, plus the existing `ParallelEdge.tsx` which is the template for the new `PulseEdge.tsx`.

## What 15e is in one paragraph

Right now the timeline view shows clips lighting up as the playhead moves through them. 15e adds a **side-by-side mini graph** of the referenced diagram, where nodes glow when one of their events is active, and edges pulse (small marker travels along the path) when their source node is active. This is the "signal travels through the modules" moment — the DAW analogy made literal. The implementation plan covers files touched, layout, the derivation logic for `activeNodeIds` / `pulsingEdgeIds`, and SVG-animation tricks.

## Conventions and watch-outs

- **pnpm**, not npm. `pnpm --filter loom-spec <script>` to run anything package-scoped.
- **Schema-first**: TypeScript types are autogenerated from `packages/loom-spec/schema/*.schema.json` via `pnpm --filter loom-spec generate-types`. Don't hand-edit `src/types/*.ts`.
- **Validation everywhere**: server PUTs validate against the schema before writing. Client-side validation runs on every diagram edit. Drift check (`loom-spec validate`) walks `code_refs`. If you add a new file kind or field, update the schema first.
- **Animation**: use `setInterval(16)` for animation loops, NOT `requestAnimationFrame`. We learned the hard way that rAF is throttled to ≈0 in iframes / hidden tabs, which broke playback verification. setInterval keeps running and is visually indistinguishable at 60Hz.
- **The view's URL-hash router**: `useViewState()` parses `location.hash` like `#diagram:overview` or `#timeline:todo-completion`. Empty hash → `diagram:overview`. Pass `ViewState` (`{ kind, id }`) to `navigate()` to switch.
- **`TimelineEvent` and `TimelineTrack`** are the autogenerated type names (intentionally not `Event`/`Track` to avoid shadowing the global DOM `Event`).
- **Diff stability**: server writes JSON with `JSON.stringify(data, null, 2) + "\n"` and self-write suppression (1.5s) so the UI doesn't react to its own saves. Don't break this.
- **MCP server vs. HTTP server**: separate processes. The MCP server doesn't have access to the chokidar watcher — that's fine; MCP writes are external to the UI, so chokidar picks them up and the UI refetches via SSE.

## Tooling notes

- You have a Preview tool that can render the dev UI and run JS in it. The dev server config is at `.claude/launch.json`. Use the `loom-spec-view` config — it runs `pnpm --filter loom-spec dev` which boots both Hono (port 7778) and Vite (port 7777 with proxy to 7778).
- The example fixture lives at `examples/todo-app/.loom/`. It has 2 diagrams (`overview`, `celebration-detail`) and 1 timeline (`todo-completion`). Pre-built fully-populated demo of every feature.
- `pnpm --filter loom-spec typecheck` and `pnpm --filter loom-spec validate-examples` are both fast (~1s) — run them after changes.
- Drift findings on the example are expected: the fixture references files like `src/server/routes/todos.ts` that don't exist (it's a docs fixture, not real code). That's fine.

## What I want from you on turn 1

1. Confirm you've read the three docs.
2. Tell me which design decisions in the 15e plan you'd push back on, if any. Specifically: SVG `<animateMotion>` vs. CSS `stroke-dashoffset` for the edge pulse; resizable splitter or fixed 60/40 split; should the mini graph default to fit-view or to inherit the layout from the diagram view?
3. Then propose a build order (probably: derive sets in TimelineView → DiagramCanvas read-only mode → NodeCard glow → PulseEdge → wire it all up). I'll confirm and we go.

Tone-wise I prefer: opinionated, honest about trade-offs, push back when I'm wrong. We've been going fast — 25+ commits in the previous session. Keep that pace.

Working directory is `/Users/renejesser/Desktop/Programming - Projekte/graphical-programming`. Start by reading the three docs.
````

---

## How to use this

1. Open a new Claude Code chat in the project working directory.
2. Paste the entire fenced block above as your first message.
3. The assistant should read the three docs, then come back with questions or a plan. Confirm and proceed.

If the assistant skips the reading step or starts coding immediately, push back — the briefing makes clear that reading and confirming the plan comes first.
